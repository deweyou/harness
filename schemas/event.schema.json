{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/deweyou/harness/schemas/event.schema.json",
  "title": "Deweyou Harness event",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "runId", "sequence", "timestamp", "type", "traceId", "spanId", "payload", "previousHash", "hash"],
  "properties": {
    "schemaVersion": { "const": 2 },
    "id": { "type": "string", "minLength": 1 },
    "runId": { "type": "string", "minLength": 1 },
    "sequence": { "type": "integer", "minimum": 1 },
    "timestamp": { "type": "string", "format": "date-time" },
    "type": {
      "enum": ["run.created", "commitment.revised", "plan.proposed", "plan.activated", "plan.superseded", "node.ready", "node.started", "node.succeeded", "node.failed", "node.blocked", "node.cancelled", "node.skipped", "node.interrupted", "resource.activated", "resource.feedback.recorded", "evidence.recorded", "claim.opened", "claim.satisfied", "claim.invalidated", "claim.waived", "decision.recorded", "run.completed", "retrospective.generated", "resource.change.proposed", "resource.change.accepted", "resource.change.rejected"]
    },
    "traceId": { "type": "string", "minLength": 1 },
    "spanId": { "type": "string", "minLength": 1 },
    "parentSpanId": { "type": "string", "minLength": 1 },
    "idempotencyKey": { "type": "string", "minLength": 1 },
    "payload": { "type": "object" },
    "previousHash": { "type": ["string", "null"] },
    "hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  }
}
