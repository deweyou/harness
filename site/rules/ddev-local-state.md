---
title: ddev-local-state
description: Ownership, visibility, cleanup, and commit boundaries for global DDev local state under ~/.deweyou/dev.
---

<!-- Generated by scripts/sync-site-content.mjs. Do not edit directly. -->


# DDev Local State

Use this rule when a task creates, reads, explains, cleans, ignores, stages, or
commits DDev runtime state.

## Defaults

- DDev local state lives under `~/.deweyou/dev/`.
- Per-repository sessions live under
  `~/.deweyou/dev/repos/<repo-id>/sessions/<branch>/`.
- Treat DDev state as local working memory, not project source.
- Do not create project-local `.deweyou/dev/` state.
- Create or update DDev state only when DDev is explicitly invoked, when the
  repository opts into DDev as its default workflow, or when a `deweyou-cli dev`
  command is being run.
- Do not install or depend on passive global hooks for DDev activation.
- Do not commit project-local `.deweyou/dev/` if it appears; treat it as legacy
  DDev state unless the user explicitly asks to version a fixture or example.
- Do not add DDev ignores to project `.gitignore` or `.git/info/exclude` for new
  global-state installs.
- When DDev creates local state, make it visible in `doctor`, `status`, or the
  final handoff. Do not leave mysterious runtime directories unexplained.
- `clean` must remove only DDev-owned state and must explain what it removed.
- `uninstall` must remove only the current repository's global DDev state,
  legacy repo-local DDev state, exact legacy DDev git exclude lines, old
  DDev-owned passive hooks, and the runtime root when no other repository state
  remains. Leave unrelated harness agents and hooks untouched.

## Branch Sessions

- Store task state under
  `~/.deweyou/dev/repos/<repo-id>/sessions/<branch>/`.
- Keep branch session files short and temporary.
- Route durable knowledge to repository docs or `repo-memory`; do not keep it
  only in DDev session files.
- Use `graph.md` for lightweight dependency notes and `evidence.md` for claims,
  evidence, and unresolved gaps.
- Do not rely on transcript replay as the state source. Store short facts and
  links to evidence instead.

## Staging Boundary

Before commit or PR delivery:

- DDev state should normally be outside the repository and absent from
  `git status`.
- if project-local `.deweyou/dev/` appears in `git status`, leave it unstaged by
  default
- report it as legacy DDev state when relevant
